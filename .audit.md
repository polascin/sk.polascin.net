---
name: SK Polascin Audit – opakovaný komplexný audit
description: Pokyny pre cyklický audit statického PHP webu sk.polascin.net (bezpečnosť OWASP, kvalita PHP/HTML/CSS kódu, databáza citátov, SEO, prístupnosť WCAG 2.1 AA, GDPR bez zdravotných údajov, výkon, odkazy a slovenská jazyková korektúra).
---

# SK Polascin Audit – opakovaný komplexný audit

## Cieľ

Vykonávať opakované hĺbkové audity slovenského lekárskeho referenčného webu **sk.polascin.net** podľa tohto dokumentu a implementovať bezpečné, relevantné opravy a vylepšenia. Audit sa zameriava na:

- bezpečnosť konfigurácie, servera a PHP kódu (OWASP),
- kvalitu a správnosť PHP kódu (syntax, logika, SQL injection, XSS),
- technickú správnosť HTML/CSS,
- SEO a štruktúru,
- prístupnosť podľa WCAG 2.1 AA,
- responzívny dizajn a výkon,
- integritu interných a externých odkazov,
- **slovenskú jazykovú a medicínsku korektúru** (primárny jazyk webu),
- súlad s GDPR (web zámerne nezhromažďuje žiadne zdravotné údaje).

## Prehľad projektu

**sk.polascin.net** je statický informačný a vzdelávací web MUDr. Ľubomíra Polaščína (nefrológia, dialýza, transplantácia obličiek) postavený na plain PHP bez frameworku. Na rozdiel od `polascin.net` **nemá** admin rozhranie, prihlásenie, relácie, formuláre, newsletter, cookies, analytiku ani cookie banner. Skladá sa z:

- **Verejné stránky:** `index.php` (rozcestník obsahu), `refbook.sk.php` (citát + bibliografia z DB), `privacy.php` (GDPR), `terms.php` (podmienky + zdravotný disclaimer)
- **Obsahové bloky (`blocks/`, includované cez `require`):** `intersection.sk.php`, `current.sk.php`, `intro.sk.php`, `info.sk.php`, `bio.sk.php`, `promo.sk.php`, `promotions.sk.php`, `affiliate.sk.php`, `pixbanner.php`, `quote.sk.php`, `favicon.php`, `styles.sk.css.php`, `footer.sk.php`
- **Databázová vrstva:** `blocks/db.sk.php` (funkcia `sk_db()`), konfigurácia `db.config.php` (mimo repozitára, v `.gitignore`), vzor `db.config.example.php`
- **Medicínsky obsah (`files/`, includovaný cez `require`):** `nefamb.html`, `nefrologia.html`, `dialyza.html`, `purifikacia.html`, `transplantacia.html`, `usgo.html`, `lipton-tx.html`
- **Dokumenty (`docs/`):** `mel.pdf` (verejne odkazovaný a vložený cez iframe)
- **Štýly:** `styles.css` (pre `index.php`, `privacy.php`, `terms.php`) a inline `blocks/styles.sk.css.php` (pre `refbook.sk.php`)
- **Serverová konfigurácia:** `.htaccess` (bezpečnostné hlavičky, blokovanie), `robots.txt`, `sitemap.xml`
- **Deploy:** GitHub Actions `.github/workflows/deploy.yml` (push na `main` → `php -l` validácia → rsync → smoke test), `.deployignore`, `DEPLOY.md`
- **Dev nástroje (len lokálne, PHAR):** `tools/install-dev-tools.ps1`, `tools/quality.ps1`, `phpstan.neon.dist`, `.php-cs-fixer.dist.php`

Databáza `quotes` (MariaDB na Websupporte) obsahuje tabuľky `sk` (citáty) a `books` (bibliografia). Web do nej **iba číta**; o návštevníkoch sa nič nezapisuje.

Adresáre a súbory, ktoré sa pri auditoch **vždy ponechávajú** bez prepisovania obsahu (len kontrola):

- `favicon/`, `favicon.ico`
- `docs/` (PDF)
- `pix/`, `weblogo/`, `websupport_banners_sk/`, `websupport-ad-banners-sk/` (obrázky zhodné s podadresármi web rootu `polascin.net`)
- `fontawesome-*` (ak pribudne)

## Opakovaný postup auditu

1. Spusti audit podľa kontrolného zoznamu nižšie.
2. Zaznamenaj nálezy vrátane závažnosti (🔴 kritická / 🟠 vysoká / 🟡 stredná / 🟢 nízka), príčiny a odporúčanej opravy.
3. Implementuj bezpečné a relevantné opravy a vylepšenia.
4. Over zmeny relevantnými kontrolami:
   - `php -l <file.php>` pre všetky zmenené PHP súbory (a rekurzívne pre celý repozitár)
   - Statická analýza (ak sú PHAR nástroje nainštalované cez `tools/install-dev-tools.ps1`):
     - `php tools/phpstan.phar analyse --no-progress`
     - `php tools/php-cs-fixer.phar fix --dry-run --diff`
   - Kontrola well-formed XML v `sitemap.xml` (`php -r 'new DOMDocument()->load(...)'`)
   - Manuálna kontrola HTTP hlavičiek a stavových kódov: `curl -I https://sk.polascin.net/`
   - Kontrola blokovania: `blocks/`, interné súbory a `db.config.php` musia vracať 403; `privacy.php`, `terms.php`, `sitemap.xml` musia vracať 200
   - Funkčná kontrola citátov (`refbook.sk.php?data=<id>`) a rozcestníka
   - **Kontrola všetkých webových odkazov** (pozri sekciu 7) — interné ciele, `href`/`src`/`iframe`, `sitemap.xml`, HTTP stav a oprava nefunkčných adries
5. Zapíš výsledky, vyriešené nálezy a poznatky do tohto `.audit.md` ako nový „Beh #X“ s odvolávkami na opravené súbory.
6. Opakuj kroky 1 až 5, kým audit nenájde žiadne nové bezpečne opraviteľné chyby.

## Kontrolný zoznam auditu

### 1. Bezpečnosť

#### HTTP hlavičky v `.htaccess`

- `X-Frame-Options: SAMEORIGIN`
- `X-Content-Type-Options: nosniff`
- HSTS: `max-age=31536000; includeSubDomains; preload`
- `Referrer-Policy: strict-origin-when-cross-origin`
- `Permissions-Policy` bez zbytočných oprávnení (`geolocation`, `microphone`, `camera`, `payment`, `usb`)
- `Cross-Origin-Opener-Policy: same-origin`
- `X-Permitted-Cross-Domain-Policies: none`
- `X-Powered-By` sa odstraňuje (`Header unset`/`unset X-Powered-By`)
- `Options -Indexes`, `ServerSignature Off`
- Blokovanie `.git`, `.env`, `*.pem`, `*.key`, `*.p12`, `*.pfx`, `*.log`, `*.sql`, `*.bak`, `phpstan.*`, `.php-cs-fixer.*`, `db.config*.php`, `*.md`
- Blokovanie interných adresárov `.git`, `.claude`, `.vscode`, `blocks`, `docs`, `files`
- **Pozor:** verejne odkazované súbory (napr. `docs/mel.pdf` vložený cez iframe/odkaz) nesmú byť súčasne blokované; ak sú, buď ich presunúť mimo blokovaný adresár, alebo pre ne spraviť výnimku.
- CSP: `img-src` povoľuje `'self'` plus affiliate bannery (`cloudfront.net`, `martinus.sk`); logá, `pix/` a Websupport bannery sú na vlastnej doméne. Ak sa CSP mení, musí zohľadniť iframe `docs/mel.pdf` a inline `<style>` v blokoch.

#### PHP bezpečnosť

- Všetky DB dotazy používajú prepared statements s viazanými parametrami (`bind_param`) — žiadna interpolácia vstupu do SQL.
- Vstup z `$_GET`/`$_POST` sa validuje (napr. `refbook.sk.php?data` ako kladné celé číslo cez `filter_input(... FILTER_VALIDATE_INT)`).
- Všetky dynamické výstupy sa escapujú cez `htmlspecialchars(..., ENT_QUOTES, 'UTF-8')`; bohatý bibliografický text prechádza allowlistom značiek.
- Prihlasovacie údaje k DB nie sú v kóde — načítavajú sa z `db.config.php` (mimo repozitára). `blocks/db.sk.php` pri chybe vracia `null` a chyby loguje cez `error_log`, nikdy ich nevypisuje návštevníkovi.
- `require`/`include` používajú len statické, nie používateľom riadené cesty.
- Žiadne tajné hodnoty (heslá, kľúče) nie sú commitnuté v repozitári ani v histórii pracovnej kópie.

#### JavaScript

- Jediný JS je inline v `blocks/current.sk.php` (dátum/čas, Swatch beat). Neprijíma používateľský vstup a needituje `innerHTML` z externých dát.
- Web nepoužíva `localStorage`, `sessionStorage`, cookies ani analytiku.

#### `.gitignore`

- Obsahuje `db.config.php`, `.env`, `*.env`, `*.pem`, `*.key`, `*.p12`, `*.pfx`, `*.log`, `.claude/`, `tools/*.phar` a ďalšie citlivé/generované súbory.
- Žiadne tajné hodnoty ani cache/log súbory nie sú commitnuté.

### 2. HTML

- Platný HTML5 doctype, `lang="sk"` na `<html>`.
- Sémantické prvky tam, kde to dáva zmysel (`main`, `nav`, `h1`–`h3`).
- Správne zatváranie značiek, žiadne duplicitné `id`.
- Všetky `<img>` majú zmysluplný `alt`; dekoratívne obrázky `alt=""`.
- Odkazy `target="_blank"` na externé weby majú `rel="noopener noreferrer"`.
- Ciele interných kotiev existujú.
- `mailto:` odkazy sú správne (`mailto:`, nie `mailto://`).

### 3. SEO

- Meta tagy: `description`, `keywords`, `author`, `copyright`, `viewport`.
- Robots meta: rozcestník a obsah indexovateľné; `privacy.php` a `terms.php` majú `noindex, follow`.
- Kanonická URL na `privacy.php` a `terms.php` je správna a konzistentná.
- `sitemap.xml` je well-formed, obsahuje aktuálny `lastmod` a len indexovateľné stránky; `robots.txt` naň odkazuje.
- `robots.txt` blokuje interné adresáre (`blocks/`, `docs/`, `files/`, `.git/`) a agresívne scrapery.
- `<title>` je popisný a konzistentný.

### 4. Prístupnosť (WCAG 2.1 AA)

- Kontrast textu min. 4.5:1 (3:1 pre veľký text).
- Viditeľný focus pre klávesnicovú navigáciu.
- Logická štruktúra nadpisov (jeden `h1` na stránku, bez preskakovania úrovní).
- Informácie sa nevyjadrujú len farbou.
- Ikonové/obrázkové odkazy majú textovú alternatívu.
- Dostatočná veľkosť dotykových cieľov (min. 44 × 44 px) pre odkazy a tlačidlá.

### 5. Responzívny dizajn

- `meta viewport` je prítomný na všetkých stránkach.
- Obsah sa škáluje do 320 px bez horizontálneho pretečenia.
- `max-width` na obrázkoch a širokých prvkoch (tabuľky, PDF iframe, bibliografia).
- Právne stránky (`.legal`) majú funkčný mobilný breakpoint.

### 6. Výkon

- Cache-busting query string pre `styles.css` (`?v=...`).
- Externé obrázky sa načítavajú len tam, kde treba; zvážiť `loading="lazy"` pre obrázky mimo viewportu.
- DB dotazy sú viazané parametrami, bez zbytočných `SELECT *` a bez N+1.
- Žiadne zbytočné externé skripty ani blokujúce zdroje.

### 7. Integrita odkazov a ich oprava

Každý beh auditu **musí** skontrolovať všetky webové odkazy vo verejne vykresľovanom kóde a **opraviť** tie, ktoré sú nefunkčné, zastarané alebo zbytočne nezabezpečené. Zakomentovaný kód sa do živej inventúry nezarátava (môže sa spomenúť ako poznámka).

#### 7.1 Inventúra

Z verejných stránok (`index.php`, `refbook.sk.php`, `privacy.php`, `terms.php`) a z includovaných blokov/súborov (`blocks/*`, `files/*` okrem zakomentovaných) zozbieraj unikátne ciele:

- `href` na `<a>` a `<link>` (vrátane `mailto:`, kanonických URL a faviconov),
- `src` na `<img>`, `<script>`, `<iframe>`,
- `loc` a `xhtml:link` v `sitemap.xml`,
- interné `require`/`include` cesty.

Relatívne cesty sa overujú voči koreňu webu (`https://sk.polascin.net/`).

#### 7.2 Kontrola

- **Interné ciele** existujú v repozitári (súbory, kotvy `#id`, `require`/`include`).
- **HTTP stav** každého unikátneho `http(s)://` odkazu a obrázka: nasleduj presmerovania; úspech je finálny 2xx. 401/403 s nadviazaným spojením (WAF, paywall) nie je automaticky mŕtvy odkaz — over, či adresa dáva zmysel. 404/410, DNS zlyhanie a timeout sú nálezy na opravu.
- **`https://` namiesto `http://`**, ak HTTPS cieľ funguje (rovnaký obsah / 2xx alebo rozumné presmerovanie). Affiliate skratky (`blinki.st` a pod.) nemeniteľ, ak by sa stratil tracking.
- **`target="_blank"`** má `rel="noopener noreferrer"`; `mailto:` nemá `target="_blank"` ani `mailto://`.
- Partnerské (affiliate) odkazy majú správny formát; ich prítomnosť je zverejnená v `privacy.php`/`terms.php`.
- Odkazy medzi stránkami rodiny webov (`intersection.sk.php`) sú platné.
- URL v `sitemap.xml` sú dostupné (200) a indexovateľné.

#### 7.3 Oprava

- Nefunkčný odkaz nahradiť aktuálnou oficiálnou URL tej istej organizácie/zdroja (overiť HTTP 2xx).
- Ak náhrada neexistuje, odkaz odstrániť alebo (ak ide o podstatný obsah) zdokumentovať výnimku v behu auditu — nenechávať mŕtvy `href`.
- Opraviť preklepy v URL, zlé schémy, rozbité relatívne cesty a chýbajúce `rel`.
- Po oprave znova overiť zmenené URL.

### 8. Slovenská jazyková a medicínska korektúra ⭐

- Diakritika a gramatika (á, é, í, ó, ú, ý, ä, č, š, ž, ľ, ĺ, ŕ, ô, dz, dž).
- Konzistentná slovenská medicínska terminológia (nefrológia, dialýza, hemodiafiltrácia, peritoneálna dialýza, transplantácia obličiek, eGFR, CKD).
- Odborný, zrozumiteľný štýl; konzistentné názvy a tituly (MUDr. Ľubomír Polaščín).
- Bez preklepov a typografických chýb; správne pomlčky a úvodzovky.

### 9. GDPR a právne dokumenty

- `privacy.php` presne odráža skutočnosť: web nenastavuje cookies, nemá analytiku ani formuláre; jediné spracúvanie sú serverové logy a e-mailová komunikácia.
- Všetky vložené prvky tretích strán (affiliate bannery Audiolibrix/CloudFront a Martinus) sú zverejnené; ClustrMaps bol odstránený. Logá, medicínske ilustrácie a Websupport bannery sa načítavajú z vlastnej domény.
- Web zámerne **nezhromažďuje žiadne zdravotné údaje**; toto je v `privacy.php` explicitne uvedené.
- `terms.php` obsahuje zdravotný disclaimer (obsah je informačný, nenahrádza lekársku starostlivosť, nevzniká vzťah lekár–pacient), autorské práva, obmedzenie zodpovednosti a rozhodné právo SR.
- Prevádzkovateľ a kontakt (`lubomir@polascin.net`) sú uvedené; dozorný orgán (ÚOOÚ SR) je uvedený.

### 10. Formátovanie a linting

- **PHP:** `php -l` pre všetky `.php` súbory; manuálna kontrola logiky, SQL a escapovania výstupov.
- **Statická analýza (voliteľné, ak sú PHAR nainštalované):**
  - `php tools/phpstan.phar analyse --no-progress`
  - `php tools/php-cs-fixer.phar fix --dry-run --diff` (a `fix` na aplikovanie)
- **Konzistencia:** kódovanie UTF-8 bez BOM, jednotné odsadenie, žiadne trailing whitespace ani merge-conflict markery.

## Bezpečnostné pravidlá

- Nevykonávaj nebezpečné alebo nekompatibilné automatické opravy bez posúdenia dopadov.
- Pri bezpečnostných nálezoch uprednostni obranné riešenia a minimalizuj riziko regresie (prepared statements, validácia vstupov, escapovanie výstupov, blokovanie citlivých ciest).
- Známe výnimky a nálezy, ktoré nie je možné bezpečne opraviť, zdokumentuj s odporúčaným ďalším postupom.
- Nezverejňuj citlivé údaje, kľúče ani heslá; nikdy necommitni `db.config.php`.
- Neznižuj bezpečnostné nastavenia `.htaccess`, `robots.txt` ani `.gitignore` len kvôli pohodliu.

## Dokončenie auditu

Po úspešnom ukončení všetkých auditných kôl (pozri aj povinné kroky v [`.doaudit.md`](.doaudit.md)):

1. Skontroluj finálny diff a stav repozitára (`git status`, `git diff --stat`).
2. Uisti sa, že všetky PHP súbory prechádzajú `php -l`, `sitemap.xml` je well-formed a všetky živo vykresľované webové odkazy sú skontrolované a opravené (sekcia 7).
3. **Vždy** commitni relevantné zmeny a **pushni na `main`** (bez `[skip deploy]`), aby sa spustil automatický deploy cez GitHub Actions.
4. Over nasadenie na produkcii: úspešný GitHub Actions run, HTTP odpoveď, bezpečnostné hlavičky a stavové kódy kľúčových ciest (`/`, `/privacy.php`, `/terms.php`, `/sitemap.xml` → 200; `blocks/`, `db.config.php` → 403; neplatný citát → 404).

## Jazykové nastavenia

- **Predvolený (a jediný) jazyk webu:** slovenčina (SK)
- **Jazyk komunikácie agenta:** slovenčina (SK), pokiaľ používateľ nepoužije iný jazyk
- Povinná kontrola diakritiky, gramatiky a slovenskej medicínskej terminológie.

## Nástroje a technológie

- PHP syntax lint (`php -l`) a manuálna bezpečnostná kontrola
- MariaDB / MySQL cez `mysqli` s natívnymi prepared statements (len čítanie)
- HTML5 sémantická a CSS3 analýza (breakpoints, responzívnosť)
- WCAG 2.1 AA kontrolný zoznam
- OWASP kontrolný zoznam HTTP bezpečnostných hlavičiek
- PHPStan a PHP-CS-Fixer (voliteľne, cez PHAR v `tools/`)
- Slovenská medicínska jazyková korektúra
- HTTP kontrola a oprava interných aj externých odkazov (`href`, `src`, `iframe`, `sitemap.xml`)

---

## Beh #1 – 2026-08-26

### Vykonané kontroly

- `php -l` pre všetky sledované `.php` súbory — bez syntaktických chýb (aj po opravách).
- Well-formed kontrola `sitemap.xml` cez `DOMDocument` — v poriadku.
- Kontrola citlivých hodnôt (`git grep`) — v kóde nie sú žiadne prihlasovacie údaje; staré uniknuté heslo je po rotácii neplatné, nové heslo nie je v repozitári.
- Kontrola HTTP hlavičiek na produkcii cez `GET` — všetkých 7 bezpečnostných hlavičiek prítomných (`X-Frame-Options`, `X-Content-Type-Options`, HSTS, `Referrer-Policy`, `Permissions-Policy`, `Cross-Origin-Opener-Policy`, `X-Permitted-Cross-Domain-Policies`).
- Kontrola blokovania na produkcii — `blocks/`, `files/`, `db.config.php`, `db.config.example.php`, `phpstan.neon.dist` → 403; `/`, `/privacy.php`, `/terms.php`, `/sitemap.xml` → 200.
- Kontrola `noindex, follow` a `rel="canonical"` na `privacy.php` a `terms.php` — prítomné.
- Kontrola BOM na vstupných PHP súboroch — žiadny BOM.
- Lokálne vykreslenie `index.php`, `refbook.sk.php?data=1`, `privacy.php`, `terms.php` cez `php -S` — 200, žiadne PHP warningy.

### Nálezy a opravy

1. 🔴 **Verejný PDF dokument bol na produkcii neprístupný (403).** `docs/mel.pdf` je na hlavnej stránke odkazovaný aj vložený cez `<iframe>`, no pravidlo v `.htaccess` blokovalo celý adresár `docs/` (`RewriteRule ^(...|docs|...) - [F,L]`), takže sekcia „Mimotelová eliminačná liečba“ vracala 403.
   - Opravené v `.htaccess`: `docs/` zostáva chránený (bez prehľadávania), ale `docs/mel.pdf` je ako jediný súbor ponechaný dostupný cez negatívny lookahead `^docs/(?!mel\.pdf$).*`.
2. 🟠 **Reverse tabnabbing — chýbajúci `rel="noopener noreferrer"`.** 96 externých odkazov s `target="_blank"` v blokoch nemalo `rel`.
   - Opravené vo `blocks/affiliate.sk.php`, `bio.sk.php`, `current.sk.php`, `footer.sk.php`, `info.sk.php`, `intersection.sk.php`, `intro.sk.php`, `promo.sk.php`, `promotions.sk.php`. Zakomentované odkazy, `mailto:` a interné `target` (`_self`/`_top`/`_parent`) sa zámerne neupravovali.
3. 🟠 **Neplatná schéma `mailto://`.** `blocks/bio.sk.php` mal `href="mailto://lubomir@polascin.net"` (dvojlomka je nesprávna a časť klientov ju neotvorí).
   - Opravené na `href="mailto:lubomir@polascin.net"`; odstránené zbytočné `target="_blank"` na mailto odkaze.
4. 🟢 **Slovenský preklep.** Atribút `title` iframe v `blocks/info.sk.php` mal „Mimotelolvá“ namiesto „Mimotelová“.
   - Opravené.

### Zostávajúce poznámky / odporúčania

- Oprava `docs/mel.pdf` v `.htaccess` používa PCRE negatívny lookahead; po nasadení treba na produkcii overiť, že `docs/mel.pdf` → 200 a `docs/` (výpis) → 403. Ak by stack Websupportu lookahead nepodporoval, náhradné riešenie je odstrániť `docs` z blokovacieho pravidla a spoľahnúť sa na `Options -Indexes`.
- Web nemá `Content-Security-Policy`. Pridanie by si vyžadovalo povoliť externé obrázky (`polascin.net`, `cloudfront.net`, `martinus.sk`), `iframe` z vlastnej domény a inline `<style>`/`<script>` v blokoch; nepridané, aby sa predišlo regresii. Kandidát na samostatné kolo.
- Zvážiť `loading="lazy"` na obrázkoch mimo úvodného výrezu (logá partnerov v `promo.sk.php`, `footer.sk.php`) pre výkon; nízka priorita.
- `refbook.sk.php` závisí od serverového `db.config.php` (mimo repozitára) — na produkcii je prítomný a funkčný (citáty sa načítavajú).
- Zmeny sú commitnuté lokálne; nasadenie na produkciu prebehne po `git push` na `main` (automatický deploy cez GitHub Actions).

---

## Beh #2 – 2026-08-26: Content-Security-Policy

### Vykonané kontroly

- Inventúra všetkých externých zdrojov (img/script/iframe/link) v blokoch a stránkach — obrázky z `polascin.net`, `d2emjept89nv7b.cloudfront.net` (Audiolibrix) a `www.martinus.sk`; iframe `docs/mel.pdf` z vlastnej domény.
- Kontrola inline event handlerov (`onclick`, `onload`, …), `javascript:` URL a `data:` URI — žiadne (okrem zakomentovaného `files/lipton-tx.html`, ktorý sa nevykresľuje).
- `php -l blocks/current.sk.php` — OK; `node --check js/current.js` — OK.
- Lokálne vykreslenie `index.php`/`refbook.sk.php` — 200, `/js/current.js` → 200, žiadne inline funkcie v HTML výstupe, cache-busting `?v=<filemtime>` funguje.

### Nálezy a opravy

1. 🟠 **Chýbajúca Content-Security-Policy.** Web nemal žiadnu CSP hlavičku (odporúčanie z Behu #1).
   - Pridaná do `.htaccess` (`Header always set Content-Security-Policy`): `default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' https://polascin.net https://d2emjept89nv7b.cloudfront.net https://www.martinus.sk; frame-src 'self'; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests`.
2. 🟡 **Inline `<script>` bránil prísnemu `script-src`.** Aby CSP nemusela povoliť `script-src 'unsafe-inline'`, bol inline skript (dátum/čas, Swatch beat) vyňatý z `blocks/current.sk.php` do nového súboru `js/current.js`.
   - `blocks/current.sk.php` teraz načítava `<script src="/js/current.js?v=<filemtime>" defer>`. Skript je nezmenený, len presunutý.

### Zostávajúce poznámky / odporúčania

- `style-src 'unsafe-inline'` je nutné ponechať — web má množstvo inline `style=""` atribútov a `<style>` blok v `blocks/styles.sk.css.php`; ich úplné odstránenie by bol veľký refaktoring. `script-src` zostáva prísny (`'self'`), čo je hlavná ochrana proti XSS.
- Po nasadení treba na produkcii overiť hlavičku `Content-Security-Policy` a v prehliadači skontrolovať konzolu na prípadné CSP porušenia (obrázky, PDF iframe, `/js/current.js`).
- `upgrade-insecure-requests` sa týka len podzdrojov a same-origin navigácie; externé `http://` odkazy v `<a>` (navigácia) zostávajú funkčné.

---

## Beh #3 – 2026-08-28: odkazy, jazyk, prístupnosť

`.agent.md` a `.audit.md` nie sú totožné (iný účel, štruktúra aj obmedzenia) — `.agent.md` ostáva. Do kontrolného zoznamu (sekcia 7) pribudla povinná inventúra, HTTP kontrola a oprava všetkých živých webových odkazov.

### Vykonané kontroly

- `php -l` pre sledované `.php` súbory — bez syntaktických chýb; `node --check js/current.js` — OK.
- Well-formed kontrola `sitemap.xml` — v poriadku; `lastmod` aktualizovaný na 2026-08-28.
- Kontrola citlivých hodnôt (`git grep`) — v sledovaných súboroch nie sú heslá ani kľúče (len odkazy na GitHub Secrets a premenné v `tools/ftp_deploy.py`).
- Inventúra 206 unikátnych `href`/`src`/`loc` vo verejne vykresľovanom kóde (zakomentovaný kód vynechaný) a HTTP overenie cez `curl` (presmerovania, timeout 18 s).
- Produkčné bezpečnostné hlavičky na `https://sk.polascin.net/` — všetky prítomné vrátane `Content-Security-Policy`; `docs/mel.pdf` → 200.
- PHPStan/PHP-CS-Fixer PHAR v tomto prostredí nie sú nainštalované — preskočené.

### Nálezy a opravy

1. 🟠 **Nefunkčný oficiálny odkaz PTN.** `https://www.ptnefro.pl/` má neplatný TLS certifikát (SNI/názov). Aktuálna stránka Polskiego Towarzystwa Nefrologicznego je `https://ptnefro.com.pl/` (HTTP 200).
   - Opravené v `blocks/promo.sk.php`.
2. 🟠 **Presmerovanie eVÚC na novú doménu.** `http://www.evuc.sk/` síce končí na `https://www.e-vuc.sk/`, ale `https://evuc.sk/` má expirovaný certifikát.
   - Opravené na `https://www.e-vuc.sk/` v `blocks/promo.sk.php`.
3. 🟠 **Rozbité cesty ikon v webovom manifeste.** `favicon/site.webmanifest` odkazoval na `/android-chrome-192x192.png` a `/android-chrome-512x512.png` v koreni webu (produkcia 404). Súbory sú v `/favicon/`.
   - Opravené na `/favicon/android-chrome-*.png`; doplnené `name` / `short_name`.
4. 🟡 **HTTP namiesto HTTPS** tam, kde HTTPS funguje: EuroPD, G2, Grammarly, Fedora, openSUSE.
   - Opravené v `blocks/promo.sk.php`, `blocks/promotions.sk.php`, `blocks/footer.sk.php`. Affiliate skratka `http://blinki.st/…` ponechaná (presmeruje na Blinkist s trackingom).
5. 🟡 **Slovenská jazyková korektúra.** `Som našiel` → `Našiel som` (`refbook.sk.php`); `Amazon-ne` / `Ľubomír Polaščína` → `Amazone` / `Ľubomíra Polaščína` (`blocks/bio.sk.php`); `zdravotne dokumentácie` → `zdravotnej`, `učí termíny` → `určí termíny`, `seba-obsluhy` → `sebaobsluhy` (`files/nefamb.html`); `tubulointerstickálne` → `tubulointersticiálne` (`files/nefrologia.html`); `pochádzajúcimi s` → `pochádzajúcimi z` (`files/dialyza.html`); tituly Nefrologie/Nephrology v `blocks/promo.sk.php`; `alt="Skilmea"` → `Skillmea`.
6. 🟡 **Prístupnosť.** Chýbajúci viditeľný focus; dva `h1` na úvodnej stránke; dekoratívne bannery mali nekonkrétny `alt`.
   - Pridané `a:focus-visible` v `styles.css` a `blocks/styles.sk.css.php`; `O mne` zmenené na `h2`; dekoratívne obrázky v `blocks/pixbanner.php` majú `alt=""` a `loading="lazy"` (lazy aj na logách `.imgpromo`).
7. 🟢 **Cache-busting CSS cez `time()`** zbytočne znemožňoval cache pri každej návšteve.
   - `index.php`, `privacy.php`, `terms.php` používajú `filemtime` ako `js/current.js`.
8. 🟢 **Drobnosti:** odstránený `target="_blank"` na `mailto:` v pätičke; dvojitá bodkočiarka v `blocks/bio.sk.php`; nepoužívaná `getWeekNumber` v `js/current.js`; v `privacy.php` doplnená `Content-Security-Policy` medzi uvedené bezpečnostné hlavičky.

### Zostávajúce poznámky / odporúčania

- `https://nephro.site/` z tohto prostredia neodpovedá na DNS (timeout) — odkaz ponechaný, ide o vlastnú lokalitu; overiť DNS/hosting mimo tejto siete.
- `http://polascin.org/` funguje a presmeruje na `https://polascin.net/`; `https://polascin.org/` má zlý certifikát. Ponechané ako HTTP záznam rodiny webov.
- HTTP 403/429/999 pri G2, NordVPN, 23andMe, Medscape, ERA, SIN, MX Linux, W3C Validátore, Amazone a LinkedIn sú ochrana pred botmi / rate limit, nie dôkaz mŕtveho odkazu pre návštevníka.
- `https://www.europd.com/` odpovedá 202 (nie 200), stránka je však dostupná cez HTTPS.
- Pätička (dátum úpravy, poďakovanie FOSS) ostáva v angličtine — zámerne technický/medzinárodný blok; preklad by bol obsahová zmena.
- `style-src 'unsafe-inline'` v CSP ostáva potrebné (inline štýly).
- PHPStan/PHP-CS-Fixer PHAR neboli v tomto behu k dispozícii.

---

## Beh #4 – 2026-09-04: jazyk, HTML, HTTP 404, výkon

### Vykonané kontroly

- `php -l` pre všetky sledované `.php` súbory — bez syntaktických chýb.
- Well-formed kontrola `sitemap.xml` — v poriadku; `lastmod` aktualizovaný na 2026-09-04.
- Kontrola citlivých hodnôt — v sledovaných súboroch nie sú heslá ani kľúče (`db.config.php` je mimo gitu).
- Produkčné bezpečnostné hlavičky na `https://sk.polascin.net/` — všetkých 8 prítomných vrátane CSP; `/`, `/privacy.php`, `/terms.php`, `/sitemap.xml`, `/docs/mel.pdf` → 200; `blocks/`, `files/`, `db.config.php` → 403.
- Inventúra ~91 unikátnych živých `http(s)` odkazov; HTTP overenie (HEAD/GET). Väčšina 2xx; 403/405/429/466 u G2, NordVPN, Amazone, LinkedIn, MX Linux, ERA, DistroWatch, 23andMe atď. sú ochrana pred botmi, nie mŕtve odkazy. Affiliate `http://blinki.st/…` a `http://polascin.org/` ponechané (tracking / zlý certifikát na HTTPS).
- Lokálne smoke: `index.php`, `privacy.php`, `terms.php` → 200; `refbook.sk.php?data=abc` → 404.
- PHPStan/PHP-CS-Fixer PHAR nie sú nainštalované — preskočené.

### Nálezy a opravy

1. 🟡 **Rozbité HTML značky v `files/transplantacia.html`.** Text „Riziká“ a „Komplikácie“ mal omylom vyňaté prvé písmeno pred `<em>` / `<strong>` (`R<em>iziká…`, `K<strong>omplikácie…`), čo kazilo sémantiku aj čitateľnosť.
   - Opravené na `<em>Riziká…</em>` a `<strong>Komplikácie…</strong>`.
2. 🟡 **HTTP 200 pri chýbajúcom citáte.** `refbook.sk.php` vracal 200 aj pri neplatnom/chýbajúcom `data`.
   - Po neúspešnom načítaní sa nastaví `http_response_code(404)` (obsah s vysvetlením zostáva).
3. 🟡 **Slovenská jazyková a medicínska korektúra.** `Amazon-e` → `Amazone` (`blocks/intro.sk.php`); `Zahrňuje` → `Zahŕňa` (`files/nefamb.html`); nadpisy s chybnou kapitalizáciou (`Klinická Nefrológia`, `Purifikácia Krvi`, `Transplantácia Obličky…`, `…Obličiek (Sonografia Ľadvín)`); `mykózny fungoides` → `mycosis fungoides`; `usiluje si` → `usiluje sa`; `zo rôznych` → `z rôznych`; `sestrička/sestričky` → `sestra/sestry` v medicínskom kontexte; LinkedIn `title`/`alt` po slovensky.
4. 🟡 **Hierarchia nadpisov (WCAG).** V `files/nefamb.html` bol `h3` pred `h2`.
   - `h2` „Nefrologická ambulancia“ je prvý; otázka je bežný odsek.
5. 🟢 **Výkon a údržba.** `loading="lazy"` na affiliate/Websupport banneroch (`blocks/affiliate.sk.php`); `.cursor/` pridané do `.gitignore` a `.deployignore`; `sitemap.xml` `lastmod` 2026-09-04.

### Zostávajúce poznámky / odporúčania

- `https://nephro.site/` z tohto prostredia neodpovedá (DNS/timeout); `https://nephrosite.polascin.net/` → 200. Odkazy na `nephro.site` ponechané (vlastná lokalita); overiť DNS/hosting mimo tejto siete.
- `http://polascin.org/` funguje; `https://polascin.org/` má zlý certifikát — bez zmeny.
- `style-src 'unsafe-inline'` v CSP ostáva potrebné (inline štýly). Obrázky sú na `'self'` (+ CloudFront/Martinus pre affiliate); `polascin.net` v `img-src` už nie je potrebné.
- PHPStan/PHP-CS-Fixer PHAR neboli v tomto behu k dispozícii.
- Po nasadení overiť na produkcii `refbook.sk.php?data=abc` → 404 a existujúci citát → 200.

---

## Beh #5 – 2026-09-04: overenie po opravách

### Vykonané kontroly

- Opätovný `php -l` všetkých `.php` súborov — OK.
- `sitemap.xml` well-formed — OK.
- Rýchla kontrola, či po opravách Behu #4 nezostali rovnaké jazykové/HTML chyby (`Amazon-e`, `Zahrňuje`, `R<em>`, `K<strong>`, chybné nadpisy) — žiadne zvyšky.
- Bezpečnostný model (prepared statements, escapovanie, `.htaccess`, blokovanie ciest) bez nových nálezov.

### Nálezy

- Žiadne nové bezpečne opraviteľné chyby v tomto kole.

### Zostávajúce poznámky

- Platné zostávajú výnimky z Behu #4 (`nephro.site` DNS, `style-src 'unsafe-inline'`, bot-ochrana pri HTTP kontrole niektorých partnerov).

### Commit, push a deploy

- Commity: `ae3ab89` (opravy Behu #4–5), `3b5cabb` (povinný push/deploy postup v `.doaudit.md`).
- Push na `main` úspešný; GitHub Actions [Deploy #33825724276](https://github.com/polascin/sk.polascin.net/actions/runs/33825724276) → **success** (validate + FTPS + smoke test).
- Produkčná verifikácia po deployi: `/`, `/privacy.php`, `/terms.php`, `/sitemap.xml`, `/docs/mel.pdf`, `refbook.sk.php?data=1` → 200; `blocks/`, `files/`, `db.config.php` → 403; `refbook.sk.php?data=abc` a `?data=999999999` → **404**; bezpečnostné hlavičky vrátane CSP prítomné; `sitemap.xml` `lastmod` 2026-09-04.

---

**Created:** 2026-08-26
**Updated:** 2026-09-04
**Project:** sk.polascin.net
**Status:** Active
